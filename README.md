# simple_golang_docker
I have written **Multistage** for a simple website in **Golang**.

## Instruction
To start, run the command ```docker compose up```. To see the page, go to the URL [localhost](http://localhost:8000/).
